const FooterSocialLogo = ({ logo }) => {
  return (
    <img
      src={logo}
      alt="social logo"
      className="footer__top__right__socialIcons__icon"
    />
  );
};

export default FooterSocialLogo;
